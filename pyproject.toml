[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ultimate-linux-installer"
version = "0.3.0"
description = "Modern UEFI multiboot Linux installation platform"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Ultimate Linux Installer contributors" }]
keywords = ["linux", "installer", "multiboot", "uefi", "grub"]
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "PyYAML>=6.0",
  "requests>=2.31",
  "jsonschema>=4.21",
  "psutil>=5.9",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "ruff>=0.6",
  "httpx>=0.27",
]
ui-dev = [
  "PySide6>=6.6",
  "pytest-qt>=4.4",
]

[project.scripts]
uli = "uli.main:main"

[tool.setuptools.packages.find]
where = ["app"]

[tool.setuptools.package-data]
uli = ["i18n/*.json", "ui/styles/*.qss", "ui/fonts/*", "web/static/**/*"]

[tool.setuptools.data-files]
"share/uli/schemas" = ["schemas/installation_plan.schema.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["app", "."]

[tool.ruff]
line-length = 100
target-version = "py311"
